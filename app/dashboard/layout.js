export default function DashBoardLayout({children}){
    return (
        <section className="lamrin">
            <nav></nav>
            

           {children}
           
        </section>
    )
}